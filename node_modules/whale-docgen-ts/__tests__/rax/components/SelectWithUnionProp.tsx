import { createElement, FC, RaxText } from 'rax';

interface B {
  b: number;
}

interface Props {
  stringOrBoolean: string | boolean;
  unionAndintersectionA: number | ({ a: boolean; c: boolean } & B);
  intersectionA: B & {
    d: string[];
  };
  unionA:
    | B
    | {
        d: string[];
      };
  baseIntersectionA: string & RaxText;
}

export const Select: FC<Props> = () => <div />;
