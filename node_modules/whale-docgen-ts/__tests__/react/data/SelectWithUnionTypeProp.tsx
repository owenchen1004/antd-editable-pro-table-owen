import * as React from 'react';

interface B {
  a: string;
  /**
   * @default "default from B"
   */
  b: string;
}
type D = B & {
  /**
   * @default "default from D"
   */
  b: string;
  d: string[];
};

interface Props {
  data: D;
}

export const Select: React.FC<Props> = () => <div />;
