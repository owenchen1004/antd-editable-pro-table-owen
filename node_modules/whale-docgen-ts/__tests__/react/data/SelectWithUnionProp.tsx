import * as React from 'react';

interface B {
  b: number;
}

interface Props {
  stringOrBoolean: string | boolean;
  unionAndintersectionA: number | ({ a: boolean; c: boolean } & B);
  intersectionA: B & {
    d: string[];
  };
  unionA:
    | B
    | {
        d: string[];
      };
  baseIntersectionA: string & React.ReactText;
}

export const Select: React.FC<Props> = () => <div />;
