import * as React from 'react';

interface B {
  a: string;
  b: number;
}
type D = B & {
  d: string[];
};
type E = D & {
  e: boolean;
};

interface Props {
  data: E;
}

export const Select: React.FC<Props> = () => <div />;
