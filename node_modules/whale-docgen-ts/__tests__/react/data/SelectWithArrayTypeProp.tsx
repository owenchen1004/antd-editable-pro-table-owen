import * as React from 'react';

type E = string[];

type F = [boolean, string];

interface Props {
  a: string[];
  b: Array<{
    c?: string;
    d: string;
  }>;
  e: E;
  s: [string, number];
  restProps: [string, number, ...boolean[]];
  restProps2: [string, number, ...F];
}

export const Select: React.FC<Props> = () => <div />;
