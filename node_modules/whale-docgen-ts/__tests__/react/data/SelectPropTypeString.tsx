import * as React from 'react';
import { ReactNode } from 'react';

interface A {
  a: string;
  b: number;
}
type E = string[];

type F = [boolean, string];
interface Props {
  a: string;
  b: A;
  e: E;
  reactNode: React.ReactNode;
  reactNode0: ReactNode;
  reactNodeFn: (a: ReactNode) => ReactNode;
  expandRender: (record: any) => React.ReactElement<any>;
  onExpand: (e: React.MouseEvent) => void;

  aFn: (a: A) => A;
  union: string | number | E;
  restProps0: [string, number, ...boolean[]];
  restProps1: [string, number, ...F];
  restProps2: {
    s: string;
  } & A;
}

export const Select: React.FC<Props> = () => <div />;
