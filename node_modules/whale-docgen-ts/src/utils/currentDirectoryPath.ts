import * as path from 'path';

export const getCurrentDirectoryName = (cwd: string = process.cwd()) => {
  const currentDirectoryParts = cwd.split(path.sep);

  return currentDirectoryParts[currentDirectoryParts.length - 1];
};
