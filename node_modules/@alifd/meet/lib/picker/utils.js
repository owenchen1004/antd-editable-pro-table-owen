"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.formatNewIndexByLast = exports.getIndex = exports.getDataArrayByIndex = exports.getDataArray = void 0;

var tslib_1 = require("tslib");

function getDataArray(data, value, cascade) {
  if (data === void 0) {
    data = [];
  }

  if (!cascade) {
    return data;
  }

  if (Array.isArray(data) && data.length === 0) {
    return data;
  }

  var array = tslib_1.__spreadArrays(data);

  var ret = [data];

  var values = tslib_1.__spreadArrays(value);

  if (values.length === 0 && data.length > 0) {
    values.push(data[0].value);
  }

  var i = 0;

  while (array.length > 0) {
    var _a = (array.find(function (it) {
      return it.value === values[i];
    }) || array[0]).children,
        children = _a === void 0 ? [] : _a;

    if (children.length > 0) {
      ret.push(children);
    }

    array = children;
    i++;
  }

  return ret;
}

exports.getDataArray = getDataArray;

function getDataArrayByIndex(data, indexList, cascade) {
  if (indexList === void 0) {
    indexList = [];
  }

  if (!cascade) {
    return data;
  }

  var i = 0;

  var array = tslib_1.__spreadArrays(data);

  var ret = [data];

  while (array && array.length > 0) {
    var index = indexList[i] || 0;

    if (index !== 0 && index >= array.length) {
      index = array.length - 1;
    }

    var children = array[index].children;

    if (children) {
      ret.push(children);
    }

    array = children;
    i++;
  }

  return ret;
}

exports.getDataArrayByIndex = getDataArrayByIndex;

function getIndex(dataArray, value) {
  return dataArray.reduce(function (rs, arr, index) {
    arr.forEach(function (obj, j) {
      if (obj.value === value[index]) {
        rs = rs.concat(j);
      }
    });
    return rs;
  }, []);
}

exports.getIndex = getIndex;

function formatNewIndexByLast(newIndex, lastIndex) {
  if (newIndex === void 0) {
    newIndex = [];
  }

  if (lastIndex === void 0) {
    lastIndex = [];
  }

  var isSame = true;

  if (!lastIndex.length) {
    return newIndex;
  }

  return newIndex.map(function (val, index) {
    if (!isSame) {
      return 0;
    } else if (lastIndex[index] !== undefined && val === lastIndex[index]) {
      return val;
    } else {
      isSame = false;
      return val;
    }
  });
}

exports.formatNewIndexByLast = formatNewIndexByLast;