/// <reference types="react" />
import { RaxNode } from 'rax';
import { BaseComponentAttributes, BasicDirectionType } from '../utils/types';
export interface DividerProps extends BaseComponentAttributes {
    orientation?: 'left' | 'right' | 'center';
    dashed?: boolean;
    direction?: BasicDirectionType;
    children?: RaxNode;
}
declare const Divider: {
    (props: DividerProps): JSX.Element;
    displayName: string;
};
export default Divider;
