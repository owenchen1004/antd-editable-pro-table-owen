/// <reference types="rax" />
import { ButtonProps } from '../button/button';
import { SubmitCallback } from './utils';
export interface SubmitProps extends Omit<ButtonProps, 'onClick'> {
    onClick?: SubmitCallback;
    validate?: boolean;
}
declare const Submit: import("rax").ForwardRefExoticComponent<SubmitProps & import("rax").RefAttributes<any>>;
export default Submit;
