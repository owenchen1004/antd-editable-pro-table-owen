import { createElement } from 'rax';
import View from '../view';
import Icon from '../icon';
import { FieldElement } from '../field';
import Switch from '../switch';
import './index.css';
export default class SwitchField extends FieldElement {
    getValueName() {
        return 'checked';
    }
    renderPreview(value) {
        const { prefix = 'mt-' } = this.props;
        return (createElement(View, { className: `${prefix}switch-field-preview` },
            createElement(Icon, { name: value ? 'select' : 'close' })));
    }
    renderControl() {
        return createElement(Switch, Object.assign({}, this.getProps()));
    }
}
SwitchField.displayName = 'SwitchField';
