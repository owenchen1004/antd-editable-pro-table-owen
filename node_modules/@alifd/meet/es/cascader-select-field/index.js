import { createElement } from 'rax';
import CascaderSelect from '../cascader-select';
import { FieldElement } from '../field';
export default class CascaderSelectField extends FieldElement {
    renderPreview(value) {
        return createElement(CascaderSelect, Object.assign({ value: value, disabled: true }, this.getProps()));
    }
    renderControl() {
        return createElement(CascaderSelect, Object.assign({}, this.getProps()));
    }
}
CascaderSelectField.displayName = 'CascaderSelectField';
