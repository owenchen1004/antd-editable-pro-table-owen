import { createElement } from 'rax';
import classNames from 'classnames';
import Text from 'rax-text';
import View from '../view';
const DateIndicator = (props) => {
    const { prefix = 'mt-', highlight, separator = ' ', values = [] } = props;
    const clsPrefix = `${prefix}datepicker`;
    return (createElement(View, { className: `${clsPrefix}-time-indicator` },
        createElement(Text, { className: classNames(`${clsPrefix}-time-indicator-text`, {
                [`${clsPrefix}-time-indicator--highlight`]: highlight === 0,
            }), "x-if": highlight === 0 }, values[0]),
        createElement(Text, { className: `${clsPrefix}-time-indicator-text` }, separator),
        createElement(Text, { className: classNames(`${clsPrefix}-time-indicator-text`, {
                [`${clsPrefix}-time-indicator--highlight`]: highlight === 1,
            }), "x-if": highlight === 1 }, values[1])));
};
export default DateIndicator;
