import { RaxNode } from 'rax';
import { BaseComponentAttributes } from '../../utils/types';
export interface ListItemProps extends Omit<BaseComponentAttributes, 'title'> {
    media?: RaxNode;
    title?: RaxNode | string;
    description?: string;
    subTitle?: string;
    disabled?: boolean;
    arrow?: 'right' | 'up' | 'down' | 'loading';
    extra?: any;
    children?: RaxNode;
}
declare const _default: import("rax").ForwardRefExoticComponent<ListItemProps & import("rax").RefAttributes<any>>;
export default _default;
