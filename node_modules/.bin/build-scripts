#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../@alib/build-scripts/bin/build-scripts.js" "$@"
else 
  exec node  "$basedir/../@alib/build-scripts/bin/build-scripts.js" "$@"
fi
