"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.tableMdastBuilder = void 0;
const u = require("unist-builder");
const table = (rows, align) => u('table', { align }, rows.map(row => u('tableRow', row.map(vo => {
    if (typeof vo === 'string') {
        return u('tableCell', [u('text', vo)]);
    }
    return u('tableCell', vo);
}))));
exports.tableMdastBuilder = (dataSource, columns) => table([
    columns.map(vo => vo.title),
    ...dataSource.map((item, index) => columns.map(vo => vo.render(item, index, dataSource)))
], columns.map(vo => vo.alignType));
//# sourceMappingURL=tableMdastBuilder.js.map