import { AlignType } from 'mdast';
import { Parent } from "unist";
import { Children } from 'mdast-builder';
export declare type TableCellContent = Children | string | boolean | number | undefined | null;
export interface TableColumn<Item> {
    title: TableCellContent;
    render: (row: Item, index: number, dataSource: Item[]) => TableCellContent;
    alignType?: AlignType;
}
export declare const tableMdastBuilder: <Item = unknown>(dataSource: Item[], columns: TableColumn<Item>[]) => Parent;
