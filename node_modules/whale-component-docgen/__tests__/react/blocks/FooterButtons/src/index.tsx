import * as React from 'react';

interface FooterButtonsBlockProps {
  /**
   * 初始点击事件，可以修改
   */
  onCancel?: () => void;
  /**
   * 初始点击事件，可以修改
   */
  onConfirm?: () => void;
}
/**
 * 底部按钮区块。在此写底部按钮展示逻辑，只透出的事件给业务
 */
const FooterButtonsBlock: React.FC<FooterButtonsBlockProps> = () => {
  return <div />;
};

export default FooterButtonsBlock;
