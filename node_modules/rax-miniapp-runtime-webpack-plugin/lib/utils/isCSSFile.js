"use strict";

var extRegex = /\.(css|js|wxss|acss)(\?|$)/;

module.exports = function (filePath) {
  var extMatch = extRegex.exec(filePath);
  return extMatch && ['wxss', 'acss', 'css'].includes(extMatch[1]);
};