import { isDingdingMiniapp } from "../_utils/miniappEnvApp";
import { formatSystemInfoAsync } from "../common";
import { CONTAINER_NAME } from "../_utils/constant";
var getInfo = formatSystemInfoAsync(function (options) {
  var _success = options.success,
    _fail = options.fail,
    _complete = options.complete;
  var _getInfo = isDingdingMiniapp ? dd.getSystemInfo : my.getSystemInfo;
  _getInfo({
    success: function success(res) {
      _success && _success(res);
    },
    fail: function fail(res) {
      _fail && _fail(res);
    },
    complete: function complete(res) {
      _complete && _complete(res);
    }
  });
}, CONTAINER_NAME.ALIPAY);
export default getInfo;