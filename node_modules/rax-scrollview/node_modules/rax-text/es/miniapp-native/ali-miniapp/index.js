import fmtEvent from './fmtEvent';

var noop = function () {};

Component({
  data: {},
  props: {
    className: '',
    style: '',
    selectable: false,
    space: '',
    decode: false,
    numberOfLines: 0,
    onClick: noop
  },
  methods: {
    onClick: function (e) {
      var event = fmtEvent(this.props, e);
      this.props.onClick(event);
    }
  }
});