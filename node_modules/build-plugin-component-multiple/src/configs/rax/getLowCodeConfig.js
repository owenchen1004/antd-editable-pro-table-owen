const getUMDConfig = require('./getUMDConfig');

module.exports = (context, options) => {
  const config = getUMDConfig(context, options);

  config.output.filename('index.lowcode.js');

  return config;
};
