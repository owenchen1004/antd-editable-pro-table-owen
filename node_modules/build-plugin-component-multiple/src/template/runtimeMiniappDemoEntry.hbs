/* eslint-disable no-global-assign */
import { createElement, render } from 'rax';
import DriverUniversal from 'driver-universal';

{{#each demos}}
import {{componentPascalName}}{{demoPascalName}} from '{{filePath}}';
{{/each}}

// @ts-ignore
window.__pagesRenderInfo = [
{{#each demos}}
  {
  path: 'pages/{{componentPascalName}}{{demoPascalName}}/index',
  render: () => {
  render(<{{componentPascalName}}{{demoPascalName}} />, document.body, { driver: DriverUniversal });
  },
  // eslint-disable-next-line no-return-assign
  setDocument: (value) => document = value,
  },
{{/each}}
];
