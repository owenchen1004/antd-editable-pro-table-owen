const path = require('path');
const { join } = path;

module.exports = function (rootDir) {
  return {
    DOC_DIR: join(rootDir, 'docs'),
    ES_DIR: join(rootDir, 'es'),
    LIB_DIR: join(rootDir, 'lib'),
    DIST_DIR: join(rootDir, 'dist'),
    TYPES_DIR: join(rootDir, 'types'),
    SRC_DIR: join(rootDir, 'src'),
    MINIAPP_DIR: join(rootDir, 'miniapp-preview'),
    BUILD_DIR: join(rootDir, 'build'),
    TMP_FILE_DIR: join(rootDir, 'node_modules/.rax-biz-component'),
  };
};
