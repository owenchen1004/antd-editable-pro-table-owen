import { addons } from '@storybook/addons';
import { create } from '@storybook/theming';

addons.setConfig({
  theme: create({
    base: 'light',
    brandTitle: 'FusionDesign',
    brandUrl: 'https://fusion.design',
    brandImage: 'https://dailyfusion.alicdn.com/images/OmkCCmo17m52.png',
  }),
});
