import React from 'react';
import { addParameters, addDecorator } from '@storybook/react';
import '@root/src/index.scss';
import cssVariablesTheme from 'storybook-addon-fusion-theme';
import StoryDecorator from './StoryDecorator';
import './markdown.css';

// import dark from '@alifd/theme-19629/dist/next.var.css'

addParameters({ docs: { page: null }});

// demo装饰器
export const decorators = [
  cssVariablesTheme,
  (Story, storyMeta) => {
    return (
      <StoryDecorator meta={storyMeta}>
        {Story()}
      </StoryDecorator>
    );
  },
];



