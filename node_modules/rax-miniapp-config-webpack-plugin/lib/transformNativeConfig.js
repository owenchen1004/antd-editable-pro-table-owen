"use strict";

var _fileNameMap;

var _require = require('path'),
    join = _require.join;

var safeWriteFile = require('./safeWriteFile');

var _require2 = require('./constants'),
    MINIAPP = _require2.MINIAPP,
    WECHAT_MINIPROGRAM = _require2.WECHAT_MINIPROGRAM,
    BYTEDANCE_MICROAPP = _require2.BYTEDANCE_MICROAPP,
    QUICKAPP = _require2.QUICKAPP;

var fileNameMap = (_fileNameMap = {}, _fileNameMap[MINIAPP] = 'mini.project.json', _fileNameMap[WECHAT_MINIPROGRAM] = 'project.config.json', _fileNameMap[BYTEDANCE_MICROAPP] = 'project.config.json', _fileNameMap);

module.exports = function (outputPath, nativeConfig, target) {
  if (nativeConfig && fileNameMap[target]) {
    safeWriteFile(join(outputPath, fileNameMap[target]), nativeConfig, true);
  }
};